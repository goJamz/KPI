# -----------------------------------------------------------------------------
# Approved-image freshness — compare the highest numbered registry tag with
# the configured upstream stable release. Runs where both GitLab and public
# upstream release APIs are reachable.
# -----------------------------------------------------------------------------
image_status_scan:
  stage: analysis
  environment:
    name: expedition-0-$PUBLISH_ENVIRONMENT
  tags:
    - expedition-0, $PUBLISH_ENVIRONMENT

  variables:
    PUBLISH_ENVIRONMENT: "prod"

  allow_failure: true

  script:
    - |
      set -eu

      # This runner has neither writable system storage paths nor /dev/fuse.
      # Use explicit user-owned paths and the FUSE-free vfs storage driver.
      BUILDAH_ROOT="${HOME}/.local/share/containers/kpi-vfs-storage"
      BUILDAH_RUNROOT="${CI_PROJECT_DIR}/.buildah-runroot-${CI_JOB_ID}"
      mkdir -p "${BUILDAH_ROOT}" "${BUILDAH_RUNROOT}"
      chmod 700 "${BUILDAH_ROOT}" "${BUILDAH_RUNROOT}"
      BUILDAH=(
        buildah
        --storage-driver vfs
        --root "${BUILDAH_ROOT}"
        --runroot "${BUILDAH_RUNROOT}"
      )
      REGISTRY_HOST="${CI_REGISTRY:-registry.cdso.army.mil}"
      export REGISTRY_AUTH_FILE="${CI_PROJECT_DIR}/.buildah-auth-${CI_JOB_ID}.json"

      buildah --version
      "${BUILDAH[@]}" info

      TILESERVER_IMAGE="registry.cdso.army.mil/cdso/containers/approved-base/tileserver-gl-light:5.7.0"
      TILESERVER_CONTAINER="kpi-tileserver-${CI_JOB_ID}"

      DOTNET_IMAGE="registry.cdso.army.mil/cdso/containers/approved-base/dotnet-sdk:10.0"
      DOTNET_CONTAINER="kpi-dotnet-${CI_JOB_ID}"

      cleanup() {
        "${BUILDAH[@]}" umount "${TILESERVER_CONTAINER}" >/dev/null 2>&1 || true
        "${BUILDAH[@]}" umount "${DOTNET_CONTAINER}" >/dev/null 2>&1 || true
        "${BUILDAH[@]}" rm "${TILESERVER_CONTAINER}" >/dev/null 2>&1 || true
        "${BUILDAH[@]}" rm "${DOTNET_CONTAINER}" >/dev/null 2>&1 || true
        rm -f "${REGISTRY_AUTH_FILE}"
      }
      trap cleanup EXIT

      cleanup

      if [ -z "${CI_JOB_TOKEN:-}" ]; then
        echo "[ERROR] CI_JOB_TOKEN is unavailable; cannot authenticate to ${REGISTRY_HOST}." >&2
        exit 1
      fi

      printf '%s' "${CI_JOB_TOKEN}" |
        "${BUILDAH[@]}" login \
          --username gitlab-ci-token \
          --password-stdin \
          "${REGISTRY_HOST}"

      echo "=== TileServer version through Buildah ==="
      "${BUILDAH[@]}" from \
        --pull=always \
        --name "${TILESERVER_CONTAINER}" \
        "${TILESERVER_IMAGE}"

      TILESERVER_ROOTFS="$("${BUILDAH[@]}" mount "${TILESERVER_CONTAINER}")"
      python3 -c \
        'import json,sys; print(json.load(open(sys.argv[1], encoding="utf-8"))["version"])' \
        "${TILESERVER_ROOTFS}/usr/src/app/package.json"

      echo "=== .NET SDK version through Buildah ==="
      "${BUILDAH[@]}" from \
        --pull=always \
        --name "${DOTNET_CONTAINER}" \
        "${DOTNET_IMAGE}"

      DOTNET_ROOTFS="$("${BUILDAH[@]}" mount "${DOTNET_CONTAINER}")"
      python3 -c \
        'import pathlib,sys; versions=[p.name for p in pathlib.Path(sys.argv[1]).iterdir() if p.is_dir() and all(part.isdigit() for part in p.name.split("."))]; print(max(versions, key=lambda value: tuple(map(int, value.split(".")))))' \
        "${DOTNET_ROOTFS}/usr/share/dotnet/sdk"

    - python3 .gitlab/scripts/images/collect_image_status.py

  artifacts:
    name: "image-status-${CI_PIPELINE_CREATED_AT}"
    paths:
      - image_status_reports/
    expire_in: 7 days
    when: always
